dependencies {
	compile project(':slickgraph-core');
}

jar {
	from { configurations.compile.collect{ it.isDirectory() ? it : zipTree(it) } }
	manifest {
		attributes 'Main-Class' : 'fr.caladan.slickgraph.demo.Main'
	}
}
